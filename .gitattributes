# Relatório gerado automaticamente por ydata_profiling (HTML+CSS+JS embutido) —
# conteúdo real do projeto é o Python que o gera, não o HTML em si. Exclui da
# estatística de linguagem do GitHub sem tirar o arquivo do repositório.
relatorio*.html linguist-generated=true
